{{- if and .Values.metrics.enabled .Values.metrics.dashboards.enabled }}
  {{- $files := .Files.Glob "files/dashboards/*.json" }}
  {{- range $path, $fileContents := $files }}
  {{- $dashboardName := regexReplaceAll "(^.*/)(.*)\\.json$" $path "${2}" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pihole.fullname" $ }}-{{ $dashboardName | trunc 50 | trimSuffix "-" }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "pihole.labels" $ | nindent 4 }}
  {{- if $.Values.metrics.dashboards.label }}
    {{ $.Values.metrics.dashboards.label }}: {{ ternary $.Values.metrics.dashboards.labelValue "1" (not (empty $.Values.metrics.dashboards.labelValue)) | quote }}
  {{- end }}
  {{- if $.Values.metrics.dashboards.annotations }}
  annotations:
    {{- with $.Values.metrics.dashboards.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
data:
  {{ $dashboardName }}.json: {{ $.Files.Get $path | toJson }}
{{- end }}
{{- end }}
